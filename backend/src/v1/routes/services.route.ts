import { Router } from "express";

const route=Router()
route.get("",()=>{})
route.post("",()=>{})
route.patch("/:id",()=>{})
route.delete("/:id",()=>{})
const ServicesRoute=route
export default ServicesRoute

